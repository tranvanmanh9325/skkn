/**
 * @deprecated — The User model has been promoted to System.ts.
 * This file is kept for backwards-compatibility only; update all imports to:
 *   import { User, IUser, IUserDocument } from './System'
 * or use the barrel:
 *   import { User } from './index'
 */
export { User, IUser, IUserDocument } from "./System";
export type { IPositionDocument, IRoleDocument } from "./System";
